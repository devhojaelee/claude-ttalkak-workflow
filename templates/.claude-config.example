# Claude Workflow Configuration
# Copy this file to your project root as .claude-config and modify

# === Linear Configuration ===
LINEAR_TEAM="YourTeamName"           # e.g., "100products"
LINEAR_PROJECT="YourProjectName"      # e.g., "Coffeechat"

# === Project Configuration ===
PROJECT_TYPE="framework"              # flask, nextjs, django, spring, go, rails, etc.
LANGUAGE="language"                   # python, typescript, java, go, ruby, etc.

# === Build & Test Commands ===
# Leave empty if not applicable
TEST_COMMAND="echo 'No test command configured'"
BUILD_COMMAND="echo 'No build command configured'"
LINT_COMMAND="echo 'No lint command configured'"

# === Environment File (for LINEAR_API_KEY) ===
# Path to environment file containing LINEAR_API_KEY
ENV_FILE=".env"

# === Optional: Custom Claude Workflow Instructions ===
# Additional instructions for Phase 4 (validation)
CLAUDE_WORKFLOW_CUSTOM=""

# === Optional: Branch Naming ===
# Default: feature/{ISSUE_ID}-{kebab-case-title}
# BRANCH_PREFIX="feature"
